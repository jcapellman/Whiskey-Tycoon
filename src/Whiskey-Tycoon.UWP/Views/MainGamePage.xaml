<Page
    x:Class="Whiskey_Tycoon.UWP.Views.MainGamePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Whiskey_Tycoon.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/background.jpg" Opacity="0.5" />
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />

                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Style="{StaticResource PageLabelHeaderStyle}" Text="DISTILLERY NAME" />
                <TextBlock Grid.Row="1" Style="{StaticResource PageHeaderStyle}" Margin="10,0,0,0" Text="{Binding Game.DistilleryName}" />

                <TextBlock Grid.Row="2" Style="{StaticResource PageLabelHeaderStyle}" Text="DISTILLER NAME" />
                <TextBlock Grid.Row="3" Style="{StaticResource PageSubHeaderStyle}" Margin="10,0,0,0" Text="{Binding Game.DistillerName}" />
            </Grid>

            <Grid Grid.Column="1" HorizontalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />

                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="BANK BALANCE ($K)" Style="{StaticResource PageLabelHeaderStyle}" />
                <TextBlock Grid.Row="1" Text="{Binding Game.MoneyAvailable}" Style="{StaticResource PageLabelHeaderStyle}" />

                <TextBlock Grid.Row="2" Text="BARRELS AGING" Style="{StaticResource PageLabelHeaderStyle}" />
                <TextBlock Grid.Row="3" Text="{Binding Game.BarrelsAging}" Style="{StaticResource PageLabelHeaderStyle}" />
            </Grid>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="DATE (YEAR-QUARTER)" Style="{StaticResource PageLabelHeaderStyle}" HorizontalAlignment="Right" />
                <TextBlock Grid.Row="1" Text="{Binding Game.CurrentDate}" Style="{StaticResource PageLabelHeaderStyle}" HorizontalAlignment="Right" />

                <Button Grid.Row="2" Style="{StaticResource ActionButtonStyle}" HorizontalAlignment="Right" Margin="0,10,10,0" VerticalAlignment="Top" Content="Next Quarter" Click="btnNextQuarter_Click" />
            </Grid>
        </Grid>

        <Grid Grid.Row="1" Margin="10,20,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Margin="0,0,0,0" Text="EVENTS" Style="{StaticResource PageHeaderStyle}" />

            <ListView Grid.Row="1" ItemsSource="{Binding Game.Events}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,10">
                            <Grid.Background>
                                <SolidColorBrush Color="Black" Opacity="0.5" />
                            </Grid.Background>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Margin="0,0,0,0" Style="{StaticResource ListTextBlockSubStyle}" Text="{Binding EventDate}" />
                            <TextBlock Grid.Row="1" Margin="0,0,0,0" Style="{StaticResource ListTextBlockStyle}" Text="{Binding EventText}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <Grid Grid.Row="2">
            <Button Style="{StaticResource BackButtonStyle}" Content="Options" Click="btnOptions_Click" />
        </Grid>
    </Grid>
</Page>