<local:BasePage
    x:Class="Whiskey_Tycoon.UWP.Views.ReleaseBatchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Whiskey_Tycoon.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/background.jpg" Opacity="0.5" />
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource PageHeaderStyle}" Text="{Binding ReleaseBatchName}" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="10,10,10,10">
                <Grid.Background>
                    <SolidColorBrush Opacity="0.5" Color="Black" />
                </Grid.Background>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />

                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />

                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />

                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />

                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Style="{StaticResource PageHeaderStyle}" Text="Release Metrics" />

                <TextBlock Grid.Row="1" Style="{StaticResource PageLabelHeaderStyle}" Text="Release Quality" />
                <TextBlock Grid.Row="2" Style="{StaticResource PageLabelHeaderStyle}" Text="{Binding Batch.Quality}" />

                <TextBlock Grid.Row="3" Style="{StaticResource PageLabelHeaderStyle}" Text="Batch Type" />
                <TextBlock Grid.Row="4" Style="{StaticResource PageLabelHeaderStyle}" Text="{Binding Batch.BatchType}" />

                <TextBlock Grid.Row="5" Style="{StaticResource PageLabelHeaderStyle}" Text="Number of Barrels" />
                <TextBlock Grid.Row="6" Style="{StaticResource PageLabelHeaderStyle}" Text="{Binding Batch.NumberBarrels}" />

                <TextBlock Grid.Row="7" Style="{StaticResource PageLabelHeaderStyle}" Text="Barrel Age In Years" />
                <TextBlock Grid.Row="8" Style="{StaticResource PageLabelHeaderStyle}" Text="{Binding Batch.BarrelAgeInYears}" />
            </Grid>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Style="{StaticResource BackButtonStyle}" Content="Back" Click="btnBack_Click" />
            <Button Grid.Column="1" Style="{StaticResource CenterButtonStyle}" HorizontalAlignment="Right" Content="Release the Batch" Click="btnRelease_Click"  />
        </Grid>
    </Grid>
</local:BasePage>